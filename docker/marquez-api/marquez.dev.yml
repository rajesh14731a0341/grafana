server:
  applicationConnectors:
    - type: http
      port: 5000
  adminConnectors:
    - type: http
      port: 5001

logging:
  level: INFO
  loggers:
    marquez: DEBUG

db:
  host: ${MARQUEZ_POSTGRES_HOST}
  port: ${MARQUEZ_POSTGRES_PORT}
  user: ${MARQUEZ_POSTGRES_USER}
  password: ${MARQUEZ_POSTGRES_PASSWORD}
  database: ${MARQUEZ_POSTGRES_DB}
