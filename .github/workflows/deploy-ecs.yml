name: Deploy Grafana ECS

on:
  push:
    branches:
      - grafana_cloudmap_2

jobs:
  terraform:
    name: Terraform Apply
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.6

      - name: Terraform Init
        working-directory: ./env/grafana
        run: terraform init -backend-config="bucket=redshift-data-migration-bucket" -backend-config="key=grafana/terraform.tfstate" -backend-config="region=us-east-1" -upgrade

      - name: Terraform Validate
        working-directory: ./env/grafana
        run: terraform validate

      - name: Terraform Plan
        working-directory: ./env/grafana
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        working-directory: ./env/grafana
        run: terraform apply -auto-approve tfplan
