{
  "name": "${name}",
  "type": "postgres",
  "url": "${host}:${port}",
  "access": "proxy",
  "user": "${user}",
  "database": "${database}",
  "secureJsonData": {
    "password": "${password}"
  },
  "jsonData": {
    "sslmode": "${sslmode}"
  }
}
